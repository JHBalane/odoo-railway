# Railway Umgebungsvariablen für Odoo

# Port (wird von Railway automatisch gesetzt)
PORT=8069

# PostgreSQL Datenbank (wird von Railway PostgreSQL Plugin automatisch gesetzt)
PGHOST=your-db-host.railway.internal
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your-secure-password
PGDATABASE=railway

# Odoo Admin Master Password (WICHTIG: Ändern!)
# Dieses Passwort wird zum Erstellen/Verwalten von Datenbanken benötigt
ADMIN_PASSWD=your-super-secret-admin-password

# Optional: Odoo Database Name (wenn du eine bestimmte DB nutzen willst)
# DB_NAME=odoo
